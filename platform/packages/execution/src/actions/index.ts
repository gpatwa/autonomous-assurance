/**
 * Action execution (Graph API writes).
 *
 * Performs the actual Microsoft Graph API write operations for approved
 * remediation actions. Each action type has a dedicated executor that
 * handles idempotency, error handling, and rollback semantics.
 */

export {};
