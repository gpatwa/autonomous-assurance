/**
 * Admin CLI for inspecting raw events, changes, incidents.
 *
 * Provides command-line tools for developers and operators to query
 * the event store, inspect normalized changes, review incidents, and
 * debug pipeline behavior during development and operations.
 */

export {};
