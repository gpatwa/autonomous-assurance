/**
 * Ingestion module.
 *
 * Responsible for polling raw audit events from Microsoft Graph API
 * and writing them to the raw event store. Handles pagination, delta
 * tokens, and retry logic.
 */

export {};
