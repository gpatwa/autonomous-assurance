/**
 * Blast radius module.
 *
 * Computes the impact scope of a detected incident by analyzing affected
 * users, groups, applications, and downstream permissions. Produces the
 * blast radius assessment attached to each incident.
 */

export {};
