/**
 * Planning module.
 *
 * Generates remediation plans for detected incidents. Produces a sequence
 * of proposed actions (the "remediation plan") that can be reviewed and
 * approved before execution. Plans never execute directly -- they are
 * handed off to the execution service via approval tokens.
 */

export {};
