/**
 * Normalization module.
 *
 * Transforms raw audit events into the canonical NormalizedChange schema.
 * Handles provider-specific field mapping, timestamp normalization, and
 * actor/target resolution.
 */

export {};
